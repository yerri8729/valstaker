import React from 'react'
import styles from './SelfDescription.module.css'

const SelfDescription = () => {
    return (
        <div className={styles.mainContainer}>
            Installation, configuration, and monitoring of validator nodes in PoS blockchains. Experience of 4 years.
        </div>
    )
}

export default SelfDescription